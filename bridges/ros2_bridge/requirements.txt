# Pin cyclonedds to a release with prebuilt wheels for both x86_64 and arm64
# (cyclonedds<11 is sdist-only on some platforms and needs the C library +
# CYCLONEDDS_HOME to compile from source).
cyclonedds>=0.10.5
mcap>=1.1.0
pyyaml>=6.0
